/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]

    withName: '.*:CLASSIFYREADS_SHORT:CLASSIFYREADS_COMMON:KRAKEN2_KRAKEN2'{
        ext.prefix = { "${meta.id}.kraken2" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/kraken2_bracken/short" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
           // enabled: false
        ]
    }

    withName: '.*:CLASSIFYREADS_SHORT:CLASSIFYREADS_COMMON:BRACKEN_BRACKEN'{
        ext.prefix = { "${meta.id}.bracken" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/kraken2_bracken/short" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
           // enabled: false
        ]
    }

    withName:  '.*:CLASSIFYREADS_SHORT:CLASSIFYREADS_COMMON:BRACKEN_GETTOPMATCHES' {
        ext.prefix = { { "${meta.id}.topmatches" } }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/kraken2_bracken/short" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
    }



    withName: '.*:CLASSIFYREADS_SHORT:CLASSIFYREADS_COMMON:SYLPH_PROFILE' {
        ext.prefix = {"${meta.id}.sylph_profile"}
        publishDir = [
            path: { "${params.outdir}/${meta.id}/sylph_sylphtax/short" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]

    }

    withName: '.*:CLASSIFYREADS_SHORT:CLASSIFYREADS_COMMON:SYLPHTAX_TAXPROF' {
        ext.prefix = {"${meta.id}.sylph-tax_taxprof"}
        publishDir = [
            path: { "${params.outdir}/${meta.id}/sylph_sylphtax/short" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]

    }

    withName: '.*:CLASSIFYREADS_SHORT:CLASSIFYREADS_COMMON:SINGLEM_PIPE'{
        ext.prefix = { "${meta.id}.singlem_pipe" }
        publishDir = [

            path: { "${params.outdir}/${meta.id}/singlem/short" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: '.*:CLASSIFYREADS_SHORT:CLASSIFYREADS_COMMON:SINGLEM_SUMMARISE'{
        ext.prefix = { "${meta.id}.singlem_summarise" }
        publishDir = [

            path: { "${params.outdir}/${meta.id}/singlem/short" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

     withName: '.*:CLASSIFYREADS_LONG:CLASSIFYREADS_COMMON:KRAKEN2_KRAKEN2'{
        ext.prefix = { "${meta.id}.kraken2" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/kraken2_bracken/long" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
            enabled: true
        ]
    }

    withName: '.*:CLASSIFYREADS_LONG:CLASSIFYREADS_COMMON:BRACKEN_BRACKEN'{
        ext.prefix = { "${meta.id}.bracken" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/kraken2_bracken/long" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
            enabled: true
        ]
    }

    withName:  '.*:CLASSIFYREADS_LONG:CLASSIFYREADS_COMMON:BRACKEN_GETTOPMATCHES' {
        ext.prefix = { { "${meta.id}.topmatches" } }
        publishDir = [
             path: { "${params.outdir}/${meta.id}/kraken2_bracken/long" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
    }
    withName: '.*:CLASSIFYREADS_LONG:CLASSIFYREADS_COMMON:SYLPH_PROFILE' {
        ext.prefix = {"${meta.id}.sylph_profile"}
        publishDir = [
            path: { "${params.outdir}/${meta.id}/sylph_sylphtax/long" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]

    }

    withName: '.*:CLASSIFYREADS_LONG:CLASSIFYREADS_COMMON:SYLPHTAX_TAXPROF' {
        ext.prefix = {"${meta.id}.sylph-tax_taxprof"}
        publishDir = [
            path: { "${params.outdir}/${meta.id}/sylph_sylphtax/long" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]

    }

    withName: '.*:CLASSIFYREADS_LONG:CLASSIFYREADS_COMMON:SINGLEM_PIPE'{
        ext.prefix = { "${meta.id}.singlem_pipe" }
        publishDir = [

            path: { "${params.outdir}/${meta.id}/singlem/long" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: '.*:CLASSIFYREADS_LONG:CLASSIFYREADS_COMMON:SINGLEM_SUMMARISE'{
        ext.prefix = { "${meta.id}.singlem_summarise" }
        publishDir = [

            path: { "${params.outdir}/${meta.id}/singlem/long" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }


    withName: 'GTDBTK_ANIREP' {
        ext.args   = ''
        ext.prefix = { "${meta.id}.gtdbtk_anirep" }
        publishDir = [
            path: { "${params.outdir}/report" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
            pattern: '*.{ani_closest.tsv,ani_summary.tsv}',
           /*  saveAs: { filename ->
                if (filename == 'versions.yml') return null
                // Simply remove any leading directory component
                return filename.split('/').last()
            } */
        ]
    }
    withName: 'BARRNAP' {
        ext.args   = ''
        ext.prefix = { "${meta.id}.barrnap" }
        publishDir = [
        path: { "${params.outdir}/${meta.id}/barrnap" },
        mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },


        ]
    }

    withName: 'BLAST_BLASTN' {
        ext.args   = '-outfmt "6 qseqid qlen stitle pident length qcovs qstart qend sstart send evalue bitscore stitle" -evalue 1e-5 -max_target_seqs 5'
        ext.prefix = { "${meta.id}.16S_rRNA.blast_out" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/barrnap" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },


        ]
    }

    withName: 'SSURRNA_GETTOPMATCHES' {
        ext.prefix = { { "${meta.id}.topmatches" } }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/barrnap" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
    }


    withName: 'BARRNAP_SPLITFASTABYGFF' {
        ext.args   = '-outfmt "6 qseqid stitle pident length evalue bitscore stitle" -evalue 1e-5'
        //ext.prefix = { "${meta.id}.16S_rRNA.blast_out" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/barrnap" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },


        ]
    }
    withName: 'BARRNAP_SPLITFASTABYGFF' {
        ext.args   = '-outfmt "6 qseqid stitle pident length evalue bitscore stitle" -evalue 1e-5'
        //ext.prefix = { "${meta.id}.16S_rRNA.blast_out" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/barrnap" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },


        ]
    }

    withName: 'SEQKIT_SEQ' {
        ext.args   = '--min-len 200 --max-len 2000'
        ext.prefix = { "${meta.id}.16S_rRNA.filterbylength" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/barrnap" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },


        ]
    }

    withName: CSVTK_CONCAT {
        publishDir = [
            path: { "${params.outdir}/report" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename },
        ]
    }
}
